# FindTheObject-CNN-Project-
PyTorch CNN that recognizes traffic signs and shows where the sign is in the image. Trains on “sign in the wild” images I generate by placing real sign crops onto random backgrounds, and includes experiments on padding/stride/pooling and data augmentation.
